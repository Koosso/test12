로컬 컴퓨터에서 먼저 git 저장소 생성하고 나중에 서버에 등록하기

1. 소스트리에서 'create' 클릭
2. git 저장소 위치 및 프로젝트명 설정
3. 저장소 생성 후 버전 관리할 파일 생성
4. 커밋 후 github 사이트에서 원격 저장소 생성(로컬 컴퓨터의 저장소 이름과 동일하게 생성)
5. 원격 저장소 url 복사
6. 소스트리에서 설정-원격지 주소 추가(원격지 이름은 'origin', url은 '원격지 url + 토큰 정보@')
7. push으로 정상적으로 동작하는지 확인